import { image } from 'paintr/helpers/image';
import { module, test } from 'qunit';

module('Unit | Helper | image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = image([42]);
  assert.ok(result);
});
