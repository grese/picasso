import { spinner } from 'paintr/helpers/spinner';
import { module, test } from 'qunit';

module('Unit | Helper | spinner');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spinner([42]);
  assert.ok(result);
});
